#custom nginx configuration
http {
    upstream all {
        server nodeapp1:3000;
        server nodeapp2:3000;
    }

    server {
        listen 8080;

        #routes
        location / {
            proxy_pass http://all;
        }
    }

}

events {}